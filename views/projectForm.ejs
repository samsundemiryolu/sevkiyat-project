<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title><%= project.projectName ? "Projeyi Düzenle" : "Yeni Proje Ekle" %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css" />
<style>
    
    .save-button {
      background-color: #3A7D44;
      color: white;
      padding: 8px 12px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s;
    }
    .projects-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #5F99AE;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }


  </style>

</head>
<body>
  <h1><%= project.projectName ? "Projeyi Düzenle" : "Yeni Proje Ekle" %></h1>

  <% if (error) { %>
    <div style="color: red;"><%= error %></div>
  <% } %>
  <% if (message) { %>
    <div style="color: green;"><%= message %></div>
  <% } %>

  <form action="<%= project.projectName ? '/projects/' + project.projectName + '/edit' : '/projects' %>" method="post">
    <label>Proje Adı (benzersiz):</label>
    <% if (project.projectName) { %>
      <input type="text" name="projectName" value="<%= project.projectName %>" readonly />
    <% } else { %>
      <input type="text" name="projectName" value="<%= project.projectName || '' %>" required />
    <% } %>
    <br><br>
    <label>Proje Türü:</label>
    <input type="text" name="projectType" value="<%= project.projectType || '' %>" required />
    <br><br>
    <label>Başlangıç Tarihi:</label>
    <input type="date" name="startDate" value="<%= project.startDate || '' %>" required />
    <br><br>
    <label>Bitiş Tarihi:</label>
    <input type="date" name="endDate" value="<%= project.endDate || '' %>" />
    <br><br>
    <button type="submit" class="save-button"><i class="fa-regular fa-floppy-disk"></i> Kaydet</button>
  </form>
  <br>
  <a href="/projects" class="projects-button"><i class="fa-solid fa-rotate-left"></i> Projeler</a>
</body>
</html>
