<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ana Sayfa - PTProje</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #DDA853; padding: 8px; text-align: left; }
    th { background-color: #C1CFA1; }
    /* Örnek navigation ve buton stilleri */

    .edit-button {
      display: inline-block;
      padding: 0.5rem 1rem;
      background-color: #28a745;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      margin-bottom: 1rem;
    }

    .edit-button:hover {
      background-color: #0056b3;
    }

    .add-button {
      display: inline-block;
      padding: 0.5rem 1rem;
      background-color: #28a745;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      margin-bottom: 1rem;
    }

    .add-button:hover {
      background-color: #0056b3;
    }

    .newprojects-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #5F99AE;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }

    .newprojects-button:hover {
      background-color: #0056b3;
    }
    .attendanceEntry-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #FFB433;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }

    .attendanceEntry-button:hover {
      background-color: #5F99AE;
    }
    .attendancereport-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #FFB433;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }

    .attendancereport-button:hover {
      background-color: #5F99AE;

    .projects-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #5F99AE;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }

    .projects-button:hover {
      background-color: #5F99AE;
    }

    .employees-button {
      display: inline-block;
      padding: 10px 15px;
      background-color: #D98324;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
      margin-right: 10px;
    }

    .employees-button:hover {
      background-color: #0056b3;
    }
    nav a.home-button {
      display: inline-block;
      background-color: #4CAF50;
      color: white;
      padding: 10px 15px;
      border-radius: 4px;
      text-decoration: none;
      font-weight: bold;
      margin-right: 10px;
      transition: background-color 0.3s;
    }
    nav a.home-button:hover {
      background-color: #45a049;
    }
    /* Genel save butonu stili */
    .save-button {
      background-color: #3A7D44;
      color: white;
      padding: 8px 12px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s;
    }
    .save-button:hover {
      background-color: #3A7D44;
    }
  </style>
</head>
<body>
  <h1>PUANTAJ TAKİP UYGULAMASI</h1>
  <nav>
    <ul>
      <li><a href="/" class="home-button"><i class="fa-solid fa-house"></i>Genel Anasayfa</a></li>
      <li><a href="/employees">Çalışanlar</a></li>
      <li><a href="/projects">Projeler</a></li>
      <li><a href="/attendance">Puantaj Girişleri</a></li>
      <li><a href="/attendance/reports">Puantaj Raporları</a></li>
    </ul>
  </nav>

  <h2>Devam Eden Projeler</h2>
  <% if (ongoingProjects && ongoingProjects.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>Proje Adı</th>
          <th>Proje Türü</th>
          <th>Durum</th>
        </tr>
      </thead>
      <tbody>
        <% ongoingProjects.forEach(proj => { %>
          <tr>
            <td><%= proj.projectName %></td>
            <td><%= proj.projectType %></td>
            <td>DEVAM EDİYOR</td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <p>Devam eden proje bulunmamaktadır.</p>
  <% } %>

  <h2>Belge Süreleri Biten Çalışanlar</h2>
  <% if (expiredEmployees && expiredEmployees.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>TC Kimlik No</th>
          <th>Ad Soyad</th>
          <th>Geçmiş Belge(ler)</th>
        </tr>
      </thead>
      <tbody>
        <% expiredEmployees.forEach(emp => { %>
          <tr>
            <td><%= emp.tcNumber %></td>
            <td><%= emp.name %> <%= emp.surname %></td>
            <td><%= emp.expiredDocs %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <p>Tüm çalışanların belge süreleri güncel.</p>
  <% } %>

  <p><%= message %></p>
</body>
</html>
